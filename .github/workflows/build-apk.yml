name: Build APK
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write   # de tao Release chua APK

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      # AGP tu tai platform/build-tools; cai san CMake 3.22.1 cho NDK build
      - name: Install SDK components
        run: yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "cmake;3.22.1" "platforms;android-36" "build-tools;36.0.0" > /dev/null || true

      - name: Build debug APK
        run: gradle assembleDebug --no-daemon

      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk FreediveColorStudio-v1.1-dev-debug.apk

      - uses: actions/upload-artifact@v4
        with:
          name: FreediveColorStudio-debug-apk
          path: FreediveColorStudio-v1.1-dev-debug.apk
          if-no-files-found: error

      - name: Publish release (tag apk-latest)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: apk-latest
          name: "APK build (auto)"
          prerelease: true
          files: FreediveColorStudio-v1.1-dev-debug.apk
